```\




int i1 = 'A';

   i1 = i1 + 10;

   System.out.println((char)i1);   // 형변환  int정수로 문자코드를 연산후
   								// 출력할땐 char타입으로 출력함
   								''은 char데이터타입의 데이터이고
   								'A' 의 값은 26이다
   								따라서 정수형 데이터타입int의 i1변수에 'A'을 선언하면
   								연산시 65으로 연산한다
   								i1 은 65이고 + 10이므로 값은 75이지만
   								출력시 ((char)i1) 해서 문자열(char)로 형변환해서
   								char에서 36에 해당하는 문자코드값을 문자로 출력한다
   					
   								
   								
   							
  for (int i = 65; i < 'A' + 26; i++) {      
        System.out.print((char)i);
      }      								//위와 마찬가지로 for문을 이용해
      									    //A에대한 문자코드값
                                               //65(A)부터65(A)+26값까지출력을반복
                                               //출력할때 문자열(char)로 형변환
                                               //해서 A부터 Z까지 출력
                                               
```

