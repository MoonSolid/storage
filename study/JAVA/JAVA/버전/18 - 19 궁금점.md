입력받은 값이 없을때

```java
 System.out.printf("총수업시간2(%d)? ",oldLesson.getTotalHours());
    String value = input.nextLine();   //문자열로 입력받는다
    int totalHours = oldLesson.getTotalHours();  // 이전의 값을 totalHours에 저장
    if (value.length() != 0) {  //입력받은값이 0이 아니라면
      totalHours = Integer.parseInt(value);  //totalHours변수에 입력받은정수값을 정수형으로 바꿔저장
    }
```

