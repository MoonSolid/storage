mkdir  git_test

git init  - 깃 저장소로 초기화

ls -all - 숨김파일도 전부 보여줌

git config --list

```java
user.email=rnreo20@gmail.com //이메일
user.name=MoonSolid  //아이디
core.autocrlf=true //true인경우 깃에서 개행문자를 알아서 처리해줌
```





vimrc

```
~/.vimrc
```

```
리눅스에서 사용하는 메모장 사용법
esc : 명령모드
i : 입력모드

명령모드 상태에서 : 입력하시면 명령행 모드

잘 안되면 esc부터 하고 입력

---------------------------------
입력모드에서 설정
set nu
set ts=2
set sw=2
set sts=2
set smartindent
set cindent

syntax on
filetype indent plugin on

전부 입력후  esc누름 ->  :누름  -> 명령모드
:에서 w는 저장 q는 나가기
:wq 는 저장하고 나가기
---------------------------------	

 add 하고 commit 하면 master 브랜치 생김  master는 배포용 
 
 git add README.md  - README.md만 애드함
 
 --------
 git commit
 	
  1 [add] add a README.md
  2   - initialize project repository
  3   - add a README.md
  
이후  : 하고 wq 로 저장 하면 커밋됨  
 --------
   
git log 하면 history 

git branch - 브런치 확인

git branch hotfix - 버그잡는용도로 만든 브런치

git checkout hotfix - hotfix 로 사용브런치 옮김

cat README.md  - README.md 의 내용출력

vi README.md - README.md 편집

--------

hotfix 수정내역 master에 합치기

git checkout master  - 마스터로 이동

git merge hotfix -   hotfix 브런치를  마스터 브런치(현재 브런치)에 합친다

--------

※ fast - forward 
- 사용하려면 합치려는 쪽과 합쳐지는 쪽의 commit 조상이 같아야 한다.

--------

마스터의 리드미 수정

3 third commit in master branch
4 break point in master branch

add 후 커밋 
--------

핫픽스로 체크아웃

핫픽스에서도 리드미 수정

3 third commit in hotfix branch
4 break poing in hotfix branch

add 후 커밋
---------  

https://www.gitignore.io/ - 사이트에서 입력창에 eclipse 등을 입력후 create를 하면 
해당 ignore 파일을 만들어준다

---------

다시 마스터 브런치가서 핫픽스 머지

---------

마스터 브런치에서 README.md 수정

HEAD
====== 



esc 누르고 커서옮긴후 dd누르면 해당 라인 삭제
esc 누르고 커서옮긴후 uu누르면 작업 되돌리기


리드미 수정후 다시 애드 커밋

-------
git log --graph       -     어느시점에 브런치 따져서 어느시점에 합쳐졌는지 보여줌


--------------------

yh0921k 깃허브 저장소에서 bitcamp-git-test repository  fork

watch 누르면 원격저장소의 활동 내역을 사용자에게 알려줌





-----------------------
이클립스 설정 내보내고 임포트하기 

file -> export -> general -> Preferences -> export all 전부 체크후 이름설정 -> finish하면 파일생성


이클립스 설정 적용할 pc에서 file -> import > general > Preferences -> 파일위치 지정후 가져오기
-----------------------

기존의 fork한 저장소 지우고

자신의 원격저장소 bitcamp-git-test 저장소 생성후 git 에 clone

git remote -v 
현재 저장소가 어느 저장소와 연결되어있는지 보여준다

오리지날 레파지토리를 fork 하고


 git remote add upstream(패치받을 저장소 이름) https://github.com/yh0921k/bitcamp-git-test
 - 포크해온 저장소를 원격저장소와 연결
 
 git remote remove 별칭  remote 지움
 
 ----------------
 README.md 를 생성옵션을 주면 내가 나중에 README.md 를 따로 만들어서 push하게되면 
 커밋조상이 달라서 push가 되지 않는다.
 ----------------
 
 git push origin --all 
 존재하는 브런치가 전부 올라간다
 
 git pull 은 fetch + merge 이다 (잘 사용하지 않는다.)
 
 git fetch     -   원격에서 패치받아옴
 
 git branch -a   
 
 git merge 
 
 
 
 ---
 로컬 저장소 만들고 git init
 
 원격 저장소 만들기 
 
 그리고 원격이랑 연결하기
 
 git remote add origin(별칭) 내 원격저장소주소/로컬레파지토리
 
 그리고 로컬에서 README.md 생성후 
 
 git push origin master - origin에 내 마스터 브랜치의 작업물을 push
 
 원격에서 README.md 수정 하기
 
 git fetch 하면 원격에서 작업한 결과물을 로컬의 remote로 받아온다
 
 git branch -a 해서 현재 위치한 브랜치 확인
 
 git checkout origin/master 로  origin/master로 이동
 
 git branch 위치 확인
 
 git checkout master  마스터 브랜치로 이동
 
 git merge origin/master  origin 내용을 master 로 병합
 
 그리고 다시 vi README.md 로 README.md 의 충돌 내역을 고친후에
 
 git add
 
 git commit -m "충돌 수정"  
 
 git push origin master 원격으로 푸시하기
 
 브런치를 따면 현재 위치한 브런치를 복사함 마스터를 수정하려면 마스터에서 브런치를 따면된다
 
 
 git branch -d branch명  -    브렌치 지우기  
 
 
 ----------------------------
 스크립트 짜기
 #!bin/bash
 경로
 명령어
 경로
 명령어
 .sh 
 저장후 실행하면 스크립트를 차례로 실행
  ---------------------------- 
  
 git commit --amend       
 - 가장 최신의 commit의 내용을 변경할수있다
 - 가장 최신의 commit 밖에 변경이 안되고 push 했을경우는 사용하지 못한다.
 
 
 취직 - 기업선별 (10개이상)- 작년의 채용공고를 바탕으로 기업의 채용패턴 익숙해지기
 
 금천구,가디 si 일 확률 굉장히 높음

 
  

















  

 
 
 
```

